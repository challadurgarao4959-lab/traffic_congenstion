# Frontend Environment Variables
# Rename this to .env.local for local development

# Backend API URL (used during build time with Vite)
# For production, set VITE_BACKEND_URL as Vercel environment variable
VITE_BACKEND_URL=http://localhost:8001/api
